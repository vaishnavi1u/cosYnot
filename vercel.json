{
  "installCommand": "npm --prefix frontend install",
  "buildCommand": "npm --prefix frontend run build && rm -rf public && mkdir public && cp -R frontend/dist/. public/",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "{backend/**,data/**}"
    }
  }
}
